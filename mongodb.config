# MongoDB Configuration Reference
# This file documents the MongoDB setup for FocusFlow

Database: focusflow
Host: 127.0.0.1
Port: 27017
Authentication: None (development)

# Collections Schema

## users
{
  "_id": ObjectId,
  "name": String,
  "email": String (unique, lowercase),
  "password": String (bcrypt hashed),
  "createdAt": Date,
  "updatedAt": Date
}

## analytics
{
  "_id": ObjectId,
  "user": ObjectId (ref: users),
  "subjectTimes": {
    "subject_name": Number (minutes)
  },
  "createdAt": Date,
  "updatedAt": Date
}

## playgrounds
{
  "_id": ObjectId,
  "user": ObjectId (ref: users),
  "name": String,
  "data": String (code content),
  "createdAt": Date,
  "updatedAt": Date
}

# Indexes (automatically created)
users.email (unique)
analytics.user
playgrounds.user

# Development Settings
Mongoose: ^7.0.0
Authentication: JWT (stored in localStorage)
Password Hashing: bcryptjs ^2.4.3
